import 'package:flutter/material.dart';

/// Paleta de cores do app — identidade visual centralizada
/// Conceito: gestão inteligente do agro, não folclore rural
class AppColors {
  // Cores principais
  static const verdeProfundo = Color(0xFF1B3A2D);
  static const begeAreia = Color(0xFFF5EDD6);
  static const pretoElegante = Color(0xFF111111);
  static const laranjaAmbar = Color(0xFFD47B2A);
  static const brancoSuave = Color(0xFFFAFAF8);

  // Cores por categoria de notícia
  static const mercadoVerde = Color(0xFF2D5F4A);
  static const techLaranja = Color(0xFFD47B2A);
  static const pecuariaAzul = Color(0xFF3A5A7C);
  static const estrategiaDourado = Color(0xFFB8860B);

  // Cores de estado
  static const shimmerBase = Color(0xFFE0E0E0);
  static const shimmerHighlight = Color(0xFFF5F5F5);
  static const shimmerBaseDark = Color(0xFF2A2A2A);
  static const shimmerHighlightDark = Color(0xFF3A3A3A);
}
